// Funcionamiento del selector

$(".default_option").click(function(){
  $(this).parent().toggleClass("active");
})

$(".select_ul li").click(function(){
  var currentele = $(this).html();
  $(this).parents(".select_wrap").children(".default_option").children("li").html(currentele);
  $(this).parents(".select_wrap").removeClass("active");
})



    $(document).click(function(){
    $(".default_option").parent().removeClass("active");
    // si lo deseamos podemos eliminar el evento click
    // una vez utilizado por primera vez
    // $(document).unbind("click");

    $(".default_option").click(function(event) {
    event.stopPropagation();
});
})

// counter

$('.figure-animation-products').each(function () {
        $(this).prop('Counter',100).animate({
            Counter: $(this).text()
        }, {
            duration: 90000,
            easing: 'swing',
            step: function (now) {
                $(this).text(Math.ceil(now));
            }
        });
    });

    $('.figure-animation-customer').each(function () {
            $(this).prop('Counter',500).animate({
                Counter: $(this).text()
            }, {
                duration: 90000,
                easing: 'swing',
                step: function (now) {
                    $(this).text(Math.ceil(now));
                }
            });
        });

        $('.figure-animation-travel').each(function () {
                $(this).prop('Counter',10).animate({
                    Counter: $(this).text()
                }, {
                    duration: 90000,
                    easing: 'swing',
                    step: function (now) {
                        $(this).text(Math.ceil(now));
                    }
                });
            });


            $(document).ready(function(){
      $('[data-toggle="tooltip"]').tooltip();
    });
